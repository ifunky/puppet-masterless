---
:backends:
  - yaml
:logger: console

:hierarchy:
  - "node/%{::fqdn}"
  - "%{::environment}"
  - "%{::osfamily}"
  - "%{::location}"
  - "%{::flavour}"
  - common

:yaml:
   :datadir: hieradata/%{::environment}
