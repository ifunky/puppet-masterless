---
:backends:
  - eyaml
  - yaml
:logger: console
:hierarchy:
  - "node/%{::fqdn}"
  - "%{::environment}"
  - "%{::osfamily}"
  - "%{::location}"
  - "%{::flavour}"
  - common

:yaml:
   :datadir: hieradata/%{::environment}

:eyaml:
   :datadir: hieradata/%{::environment}
