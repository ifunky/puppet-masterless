---
:backends:
  - yaml
:logger: console

:hierarchy:
  - "node/%{::fqdn}"
  - "%{::environment}"
  - "%{::osfamily}"
  - "%{::location}"
  - "%{::flavour}"
  - common

:merge_behavior: deeper

:yaml:
   :datadir: hieradata/%{::environment}
